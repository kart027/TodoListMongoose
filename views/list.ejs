
 <%- include("header") %>   
 <!-- Above thing is called layout that is if we use something very often then we can make 
 layout or make in sepreate file with extendion .ejs then use it with above syntax -->

<body>
    <!-- <% if(KindOfDay == "6"  || KindOfDay == 0){ %>
        <h1>It is weekend</h1>
  <%  }else{ %>
    <h1>It is weekday</h1>
  <%  } %> -->


    <div class="box" id="heading">
        <h1>
            <%= KindOfDay %>
        </h1>
    </div>
    <div class="box">

      <%  NewListitems.forEach(function(item){ %>
                <form action="/delete" method="post">
            <div class="item">
                <input type="checkbox"  onchange="this.form.submit()"  value="<%= item._id %>" name="checkbox">
              <p><%= item.name %></p>
            </div>
        </form>
      <%  }) %>


        <!-- <% for(var i=0;i< NewListitems.length;i++) { %>
            <div class="item">
                <input type="checkbox">
              <p><%= NewListitems[i].name %></p>
            </div>
            <% } %> -->

            <form class="item" action="/" method="post">
                <input type="text" name="num1" placeholder="Newitem" autocomplete="off">
                <button type="submit" name="submit">+</button>
            </form>
        

    </div>

    <%- include("footer") %>

    
   
</body>

</html>